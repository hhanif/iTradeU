
//*******************************Packages**********************************
var mongoose = require('mongoose');

var matchArray; //store matches' names in array
var itemToMatch require('./app/models/item');
var keyword1;


//*****************************************************************








//get keyword1
function match(item, callback)  {
  itemToMatch = item;
  keyword1 = item.keyword1;
  }

//something to parse through all items

while( //item exists ) {



  if (itemToMatch.keyword1 === possibleMatch.keyword2 ||
      itemToMatch.keyword1 === possibleMatch.keyword3) {
        //add to match list
        return
      }
    //increment to next item
  };
